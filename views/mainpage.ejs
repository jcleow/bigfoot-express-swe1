<html>
  <body>
    <ul>
      <% sightings.forEach(function(sighting,index){ %>
      <li>
        <a href="<%= `/sightings/${index}` %>"><%=sighting.YEAR%></a>
      </li>
      <% }); %>
    </ul>
  </body>
</html>
